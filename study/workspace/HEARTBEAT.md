# Heartbeat

- If there is no explicit pending task in this workspace, reply with `HEARTBEAT_OK`.
- Do not invent work.
- Do not send proactive messages to external channels unless explicitly configured and allowed.
